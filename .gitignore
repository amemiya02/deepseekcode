# Build artifacts
/bin/
/dist/
/dsc
# Root-only: an unanchored `benchrunner` also matches paths like
# bench/cmd/benchrunner/main_test.go, which silently drops tests from PRs.
/benchrunner
*.exe
*.test
*.out

# Local agent state (this binary writes here)
.deepseek/

# Editors
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Coverage
coverage.out
coverage.html

# Go workspace files
go.work
go.work.sum

# Per-project Claude state (parent dir uses this; we don't ship it)
.omc/

.pxr/
docs/superpowers/

# Benchmark output
bench/traces/
bench/reports/