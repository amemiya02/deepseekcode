id: subagent-parallel
fixture_repo: fixtures/subagent-parallel
commit: "HEAD"
prompt_file: prompts/subagent-parallel.md
timeout_seconds: 600
# Prompt asks for analysis ("List Go files / Identify exports / Find
# TODOs / Analyze coverage"). No writes are expected — enforce that
# explicitly. The previous `[.]` allow-list let any write pass.
read_only: true
success:
  tests: []
  diff_invariants:
    - no_changes: true
metrics:
  require_cache_gate: true
