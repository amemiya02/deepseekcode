// DiffIsland — diff産物 rendered as an obsidian CodeIsland (spec §3 material rule).
// DiffView already renders the island shell + `path · +N −M` header; this is the
// canonical name the spec and later phases (P3.2 approval gate) import.
/** @public — canonical spec name (§3); kept for spec/later-phase imports even when unreferenced. */
export { DiffView as DiffIsland } from './DiffView'
